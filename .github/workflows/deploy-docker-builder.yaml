name: Build & push Docker for building the ori-a website

on:
  push:
    paths:
      - 'buildtools/Dockerfile'
      - '.github/workflows/build-push-docker.yaml'
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker image
        run: |
          IMAGE_NAME=ghcr.io/${{ github.repository_owner }}/oria-builder:latest
          docker build -t $IMAGE_NAME -f builder/Dockerfile .
          docker push $IMAGE_NAME
